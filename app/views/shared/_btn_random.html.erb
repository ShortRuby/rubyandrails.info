<% @random.each do |item| %>
  <%= link_to "🍀 Random #{ title }", item, data: {:"splitbee-event" => "random btn #{ title} "}, class: "text-center flex-1 rounded-md shadow-lg shadow-red-500/20 fixed bottom-5 right-5 z-40 text-base bg-red-500/90 backdrop-blur-md px-8 py-4 hover:bg-red-600 font-custom font-bold text-white inline-block" %>
<% end %>
