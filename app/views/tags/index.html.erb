<h1 class="font-bold mb-5 font-custom text-5xl">I want to learn</h1>
<p class="max-w-xl mb-10 md:mb-20">Choose a topic, and we'll suggest books where you can learn more about it</p>

<div class="grid grid-cols-1 lg:grid-cols-4 gap-4 md:gap-10 xl:gap-16">
  <div class="col-span-4 order-2 lg:order-first grid md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 place-items-stretch gap-4 lg:gap-6">
      <% @tags.each do |tag| %>
        <%= link_to tag, class: "border rounded-sm lg:aspect-video bg-fuchsia-50 hover:bg-fuchsia-100/70 border-fuchsia-200 p-5" do %>
          <h2 class="font-bold font-custom text-fuchsia-500 hover:text-fuchsia-600 text-xl leading-tight">
            <%= tag.title %>
          </h2>
        <% end %>
      <% end %>
  </div>
</div>

<div class="fixed bottom-10 right-10 flex gap-10">
  <% if current_user.try(:admin?) %>
    <%= link_to new_tag_path  do %>
      <svg xmlns="http://www.w3.org/2000/svg" class="h-10 w-10 fill-red-500" viewBox="0 0 20 20" fill="currentColor"> <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-11a1 1 0 10-2 0v2H7a1 1 0 100 2h2v2a1 1 0 102 0v-2h2a1 1 0 100-2h-2V7z" clip-rule="evenodd" /> </svg>
    <% end %> 
  <% end %>
</div>

