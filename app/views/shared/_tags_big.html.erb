<% unless tag.nil? || tag.empty? %>
  <div class="flex gap-2 flex-wrap sm:grid sm:grid-cols-2 xl:grid-cols-3 sm:gap-1">
    <% tag.each do |tag| %>
      <%= link_to tag, class: "break-normal inline-block lg:aspect-square text-xl xl:text-3xl font-custom text-slate-700 bg-indigo-50 hover:bg-indigo-50/60 transition rounded-md p-4 sm:p-6" do %>
        <%= tag.title %>
      <% end %>
    <% end %>
  </div>
<% end %>
